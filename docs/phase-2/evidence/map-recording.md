# 地图录制阶段证据

## 自动化证据

- `MapFrameGeometryDetectorTests`、`MinimapGeometryDetectorTests` 和 `EnvironmentGeometryClassifierTests`：覆盖棕色背景过滤、小地图内容区、全局平台/梯子、角色标记，以及本地 `environment` 框分类。
- `MapRecorderTests`：覆盖 5 FPS 采样、跨帧稳定、同层平台合并、边缘/整高梯子过滤、角色邻近梯子和落脚平台佐证、连续纵向轨迹、未验证连接隔离、全平台图连通性、样本/大小/条目上限、单行 JSONL 分块、失败导出清理，以及质量结论往返。
- 严格拓扑定向回归 37/37 通过；全量 .NET：Core 32/32、Host 139/139、InputBroker 28/28 通过；React 30/30 通过。
- WindowsHost Release：0 警告、0 错误。
- 真实客户端离线帧：面板边框约束后小地图仍提取 8 个平台、2 个梯子候选、角色坐标约 `(0.50, 0.80)`；重复静态帧因没有纵向移动轨迹返回 `CONNECTIVITY_MISSING`，包仍能重新加载，未误标为可用于规划。
- 2026-08-21 20:19:57 的完整录制 `current-map-20260821-121957-5548bc3f6d5243b09e3b90a4f7d80927.mapzip` 含 377 个样本。经同一生产 `MapRecorder` 离线回放，17 个旧平台段合并并过滤为 11 个平台，12 个旧梯子候选只保留 1 个具备实际纵向轨迹和本地证据的连接；边缘及整高伪梯子未导出。该连接只关联平台 1/3，整图不连通，因此结果为 `planningReady=false`、`CONNECTIVITY_MISSING`。原始观测可继续用于诊断或补录，但当前包不能进入路径规划。

## 当前边界

录制器已接入独立实时预览窗口。点击“开始录制地图”后，预览只保存归一化几何候选和观测时间线，不读取键盘、不连接 Broker、不发送输入。结束录制会在 `%LOCALAPPDATA%/MapleProduct/map-recordings` 生成包。

全局平台以小地图为坐标来源；小地图内容区没有可验证边框时直接放弃该帧，避免深色场景改变坐标。梯子/绳索连接必须由新鲜的识别 Self、角色附近的本地梯子框、落脚平台和连续小地图纵向轨迹共同佐证。导出结果只有在平台覆盖、连接、角色轨迹和全平台连通图全部通过质量门后才标记 `planningReady`，该结论会随地图包重新加载。本记录器不会自动开启寻路；地图匹配、怪物模板绑定和闭环移动属于后续阶段。
